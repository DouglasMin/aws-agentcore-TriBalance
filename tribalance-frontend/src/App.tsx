export default function App() {
  return (
    <div style={{ padding: 40, fontFamily: 'var(--font-display)' }}>
      <h1 style={{ fontSize: 48, color: 'var(--warn)', letterSpacing: '-0.02em' }}>
        TRIBALANCE / ATLAS
      </h1>
      <p style={{ color: 'var(--ink-dim)', marginTop: 12 }}>
        scaffold up. next: grid + panels.
      </p>
    </div>
  );
}
