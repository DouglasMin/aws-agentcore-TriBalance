# Copy to .env.local (gitignored) and fill in.
# After deploying tribalance-proxy CDK stack, copy the ProxyFunctionUrl output here.
VITE_PROXY_URL=https://6cqrykgznqnsfir3clmy346ozy0ffedu.lambda-url.ap-northeast-2.on.aws

# Bearer token for the proxy Lambda. Fetch the value with:
#   aws secretsmanager get-secret-value \
#     --secret-id $(aws cloudformation describe-stacks \
#       --stack-name TriBalanceProxy \
#       --query 'Stacks[0].Outputs[?OutputKey==`AppTokenSecretArn`].OutputValue' \
#       --output text) \
#     --query SecretString --output text
# If unset, requests go without Authorization header (dev/local only).
VITE_PROXY_TOKEN=